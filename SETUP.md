# 架設指南（換人使用）

這是一個 LINE 群組用的活動布告欄。成員在 LINE 裡打開網頁，就能建立活動、報名、留言、看名單。

資料存在你自己的 Google 試算表，程式免費，不需要伺服器、不需要信用卡。

---

## 你需要準備

| 項目 | 說明 |
|---|---|
| Google 帳號 | 放試算表與後端程式（Apps Script） |
| GitHub 帳號 | 放網頁（GitHub Pages 免費） |
| LINE 帳號 | 到 LINE Developers 建立一個 LIFF 應用程式（免費） |

三個都免費。整套流程大約 40 分鐘。

> **最重要的一件事：第 5 步一定要把 `GAS_API_URL` 換成你自己的網址。**
> 沒換的話，你的網站會把活動寫進「原作者的試算表」，你自己的表反而是空的。

---

## 步驟 1：複製程式碼

在原作者的 GitHub 專案頁按 **「Use this template」→ Create a new repository**（如果沒有這個按鈕就按 **Fork**）。

Template 比 Fork 好：拿到的是乾淨的新倉庫，不會帶著原作者的開發歷史，也不會在對方專案頁顯示關聯。功能上兩者完全一樣。

倉庫名稱自取，選 **Public**（Public 才能免費用 GitHub Pages）。

你會拿到三個重要檔案：

- `index.html` — 整個網頁（HTML、CSS、JavaScript 全在這一個檔案裡）
- `version.json` — 版本記錄，讓已開啟的頁面自動更新
- `gas/Code.gs` — 後端程式，等一下要貼進 Apps Script

---

## 步驟 2：開啟 GitHub Pages，拿到網址

在你的倉庫：**Settings → Pages**

- Source 選 **Deploy from a branch**
- Branch 選 **main**、資料夾選 **/ (root)**
- 按 Save

等一兩分鐘，頁面上方會出現你的網址，長得像：

```
https://你的帳號.github.io/你的倉庫名/
```

**把這個網址記下來**，第 4 步要用。

---

## 步驟 3：建立 Google 試算表與後端

1. 到 [drive.google.com](https://drive.google.com) 新增一個 **Google 試算表**，名稱自取。
2. 在試算表選 **擴充功能 → Apps Script**。
3. 把編輯器裡原有的內容全部刪掉，貼上 `gas/Code.gs` 的**完整內容**，按存檔。
4. 左側 **專案設定** → 時區改成 **(GMT+08:00) Taipei**。
   > 這步不能跳過。前端送來的時間不帶時區，時區設錯會讓所有活動時間整批偏移。
5. 回到編輯器，上方函式下拉選單選 **`setupProject`**，按 **執行**。
   - 第一次會跳出授權畫面：選你的帳號 → 「進階」→「前往（不安全）」→ 允許。
     （這個警告是因為程式沒有經過 Google 審核，這是你自己寫的程式，正常現象。）
   - 執行成功後，回試算表會看到自動建立的三個分頁：`Events`、`EventsArchive`、`AuditLog`。
6. 部署：右上角 **部署 → 新增部署** → 類型選 **網頁應用程式**
   - 執行身分：**我**
   - 誰可以存取：**所有人**
   - 按「部署」，複製產生的 **網頁應用程式網址**（結尾是 `/exec`）

**把這個 `/exec` 網址記下來**，第 5 步要用。

---

## 步驟 4：建立 LINE LIFF

1. 到 [developers.line.biz](https://developers.line.biz/console/) 登入。
2. 建立一個 **Provider**（名稱自取，例如你的群組名）。
3. 在該 Provider 底下建立 **LINE Login** channel。
4. 進入該 channel 的 **LIFF** 分頁 → **Add**：
   - LIFF app name：自取
   - Size：**Full**
   - Endpoint URL：**填第 2 步的 GitHub Pages 網址**
   - Scopes：勾選 **profile**（必要，用來取得使用者名稱與頭像）
     與 **chat_message.write**（讓「宣傳」「分享活動」能直接發到聊天室；不勾也能用，只是會退回「複製後自己貼上」）
   - 按 Add
5. 建立後會得到兩個東西：
   - **LIFF ID**：長得像 `2010984505-Nv6CpgYO`
   - **LIFF URL**：長得像 `https://liff.line.me/2010984505-Nv6CpgYO`

**兩個都記下來**，下一步要用。

---

## 步驟 5：改設定（最關鍵的一步）

回到 GitHub，直接在網頁上編輯 `index.html`（點檔案 → 鉛筆圖示）。

要改的東西在**檔案最上面**，第 8 行附近，找 `SITE_SETUP`：

```js
window.SITE_SETUP = {
  GAS_API_URL: "貼上第 3 步的 /exec 網址",
  LIFF_ID:     "貼上第 4 步的 LIFF ID",
  LIFF_URL:    "貼上第 4 步的 LIFF URL",
  OWNER_HOSTS: ["你的帳號.github.io"]
};
```

四個值都要換：

- `GAS_API_URL` 沒換，資料會寫進**原作者的試算表**
- `LIFF_URL` 是「分享活動」按鈕產生的連結，沒換的話成員按分享會跳到原作者的頁面
- `OWNER_HOSTS` 填你自己的 GitHub Pages 網域（第 2 步那個網址的網域部分，不含路徑）

> **忘記改會怎樣？** 網頁會整頁顯示「這個網頁還沒設定完成」，不會連線也不會寫入任何資料。
> 這是刻意的保護，看到那一頁就代表這一步還沒完成。
> 之後如果改用自訂網域，記得把新網域加進 `OWNER_HOSTS`，否則你自己的網站也會被擋。

同一個檔案裡再找 `APP_VERSION`，還有頁首的版本標籤（搜尋 `version-label`），把版本字串改成當下時間，格式 `vYYMMDD.HHMM`，例如 `v260908.1430`。

然後編輯 `version.json`，改成同一個版本：

```json
{
  "version": "v260908.1430",
  "releasedAt": "2026-09-08T14:30:00+08:00"
}
```

> 三個地方的版本字串必須完全一致。這是自動更新機制用的：使用者頁面每 60 秒比對一次 `version.json`，不一樣就自動重新載入。

存檔（Commit changes）。等一兩分鐘 GitHub Pages 重新發布。

---

## 步驟 6：驗收

1. 用手機 LINE 傳給自己那個 **LIFF URL**，點開。
2. 檢查：
   - [ ] 頁面打得開，**沒有**出現「這個網頁還沒設定完成」（出現代表第 5 步沒做完）
   - [ ] 頁首右上角顯示你剛設定的版本號
   - [ ] 顯示你的 LINE 名稱與頭像（代表 LIFF 正常）
   - [ ] 建立一個測試活動 → **回 Google 試算表看 `Events` 分頁有沒有多一列**（這是最關鍵的一項，代表後端接通了）
   - [ ] 報名／取消報名正常
   - [ ] 換另一支手機打開，看得到同一個活動

第 3 項沒過，就是 `GAS_API_URL` 沒設好或部署設定不對，回第 3、5 步檢查。

---

## 選配功能

### Google 日曆自動同步

在試算表上方選單 **活動布告欄 → 設定 Google 日曆**，貼上日曆網址或 ID。
執行部署的那個 Google 帳號必須對該日曆有「變更活動」權限（唯讀訂閱的日曆不行）。
設定完可再執行「同步現有未過期活動」，把現有活動補進日曆。

### 管理員模式

試算表選單 **活動布告欄 → 設定管理員密碼**。
密碼只會以加鹽 SHA-256 雜湊存在 Script Properties，明碼不會出現在試算表或程式碼裡。

使用方式：在網頁**點擊頁首右上角的版本號**，輸入密碼即可進入管理員模式（可編輯／刪除任何人的活動）。重新載入頁面就會自動退出。

> 入口刻意藏在版本號上，不要改成明顯的按鈕。

---

## 常見問題

**Q：改了程式，但手機上還是舊的**
先直接開 `https://你的帳號.github.io/你的倉庫名/version.json` 看版本有沒有更新。
- 沒更新 → GitHub Pages 還在發布，等一下；或是 Actions 有錯誤
- 更新了但手機沒變 → LINE 內建瀏覽器的快取，完全關閉網頁再重開

**Q：改了 `Code.gs` 但功能沒變**
Apps Script 存檔不等於部署。試算表選單跑的是「編輯器裡存檔的程式碼」，但網頁連的 `/exec` 用的是**部署版本**。
要讓網頁生效：**部署 → 管理部署 → 編輯（鉛筆）→ 版本選「新版本」→ 部署**。
> 一定要用「編輯現有部署」，不要按「新增部署」——那會產生新網址，你得回去改 `index.html`。

**Q：網頁一片空白或卡在載入**
多半是 LIFF 設定問題。檢查 LIFF 的 Endpoint URL 是否與 GitHub Pages 網址完全一致（含結尾斜線），以及 `LIFF_ID` 有沒有貼錯。

**Q：一開始很快，用的人變多之後很慢**
這是 Apps Script 的先天限制：部署設定是「以我的身分執行」，所以所有使用者的請求都算同一個執行身分，Google 會排隊處理。
實測：6 個請求同時進來，每個腳本內部只跑 1～2 秒，但 HTTP 各等了 25～77 秒。
**唯一有效的解法是減少請求次數**，不是加快程式。看 `index.html` 的 `DATA_SYNC_INTERVAL_MS`（目前 60 秒，不要調回十幾秒）。
再不夠就要把讀取移出 Apps Script（例如加一層 Cloudflare Workers 快取代理）。詳見 `AGENTS.md` 的「同步、更新與快取」章節。

**Q：可以幾個人用？**
一般 LINE 群組（幾十到一兩百人）沒問題。同時「開著頁面」的人數才是關鍵，不是群組總人數。

---

## 資料在哪、怎麼備份

所有資料都在你的 Google 試算表：

| 分頁 | 內容 |
|---|---|
| `Events` | 目前顯示中的活動 |
| `EventsArchive` | 已結束／已刪除的活動（每天凌晨 4 點自動搬移） |
| `AuditLog` | 所有操作紀錄 |

備份就是「檔案 → 建立副本」。試算表本身也有完整的版本記錄可以還原。

程式不會刪你的資料：刪除活動是軟刪除（只標記 `status = deleted`），列的移除只由每日歸檔流程處理，而且會先複製到 `EventsArchive` 才刪。

---

## 想改東西的話

- 前端全部在 `index.html` 這一個檔案裡（HTML + CSS + JavaScript）
- 後端全部在 `gas/Code.gs`
- `AGENTS.md` 有完整的維護規則與踩雷紀錄（權限模型、版本規則、效能實測數據），改之前建議先讀
- `gas/README.md` 有表格欄位與部署細節
